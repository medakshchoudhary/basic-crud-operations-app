<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="main w-full min-h-screen p-10 text-white bg-zinc-900">
        <a class="text-blue-500 text-2xl tracking-tighter" href="/readUsers">Read All Users</a>
        <form action="/editUser/<%= user.id %>" method="post">
            <h1 class="mt-10 mb-5 pb-3 text-2xl">Edit User</h1>
            <input class="px-2 py-1 mr-2 outline-none rounded-md bg-zinc-800" type="text" placeholder="Name" name="name" value="<%= user.name %>">
            <input class="px-2 py-1 mr-2 outline-none rounded-md bg-zinc-800" type="text" placeholder="Email" name="email" value="<%= user.email %>">
            <input class="px-2 py-1 outline-none mr-2 rounded-md bg-zinc-800" type="text" placeholder="Image url" name="image" value="<%= user.image %>">
            <input class="bg-yellow-500 rounded-md px-2 py-1" type="submit" value="Update User">
        </form>
    </div>
</body>
</html>