<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="main w-full min-h-screen p-10 text-white bg-zinc-900">
        <a class="text-blue-500 text-2xl tracking-tighter" href="/">Go Back To Home</a>
        <h1 class="mt-10 mb-5 pb-3 text-2xl">All Users</h1>
        <div class="users flex flex-wrap gap-3">
            <% if(users.length > 0 ) { %>
                <% users.forEach(function(user){ %>
                    <div class="user p-4 rounded-md w-72 bg-zinc-800">
                        <div class="h-52 w-full rounded-md overflow-hidden bg-zinc-500">
                            <img class="object-cover w-full h-full" src="<%= user.image %>" alt="">
                        </div>
                        <h3 class="text-xl mt-3 tracking-tighter"><%= user.name %></h3>
                        <h5 class="text-zinc-500"><%= user.email %></h5>
                        <div class="flex justify-between mt-10">
                            <a href="/edit/<%= user._id %>">Edit User</a>
                            <a class="text-red-400" href="/delete/<%= user._id %>">Delete User</a>
                        </div>
                    </div>
                    <% }) %>
                <% } else { %>
                    <h3 class="text-zinc-500">No users yet.</h3>
                <% } %>
                </div>
            </div>
        </body>
        </html>